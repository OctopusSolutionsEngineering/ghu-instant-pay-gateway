apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: instant-pay-gateway-staging
  annotations:
    argo.octopus.com/project: "Instant Pay Gateway"
    argo.octopus.com/environment: "staging"
spec:
  project: default
  source:
     repoURL: https://github.com/OctopusSolutionsEngineering/ghu-instant-pay-gateway.git
     targetRevision: HEAD
     path: Kubernetes/Manifests/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:  
      prune: true
      selfHeal: true
    syncOptions:
    - Validate=false
    - CreateNamespace=true